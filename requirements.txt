# Application « PIAL — Affectation des AESH » (app.py) et chaîne historique (generer_edt.py)
# Python 3.11 recommandé. Installation :  python -m pip install -r requirements.txt

# — Application —
flask==3.1.3            # interface locale (127.0.0.1 uniquement)
waitress==3.0.2         # serveur WSGI : celui de Flask lâche des connexions sous charge
ortools==9.15.6755      # solveur CP-SAT : calcul des affectations
openpyxl==3.1.5         # classeur de recueil et exports .xlsx
icalendar==5.0.11       # lecture des exports ProNote (.ics)
pytz==2023.3

# — Chaîne historique : publication vers Google Sheets (facultative) —
gspread==5.12.0         # ne pas monter en version 6 sans adapter worksheet.update()
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
